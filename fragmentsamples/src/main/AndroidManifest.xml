<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.whyles.explore.fragmentsamples">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".basics.BasicsActivity" />
        <activity android:name=".StaticFragment.FromXmlActivity" />
        <activity android:name=".DynamicFragment.FromCodeActivity" />
        <activity android:name=".basics.NoUiFragmentActivity" />
        <activity android:name=".instance.RegisterInstance.InstanceRegisterActivity" />
        <activity android:name=".instance.TabSwitchInstance.InstanceTabSwitchActivity"></activity>
    </application>

</manifest>